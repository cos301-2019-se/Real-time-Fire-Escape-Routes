package Builder;

import org.junit.Assert;
import org.junit.Test;
public class DoorTest {


    @Test
    public void testOne() {
        /*
        double [][] array = new double[3][2];
        for(int i = 0; i < 3; i++)
        {
            array[i][0] = 0;
            array[i][1] = 10;
        }
        Door d = new Door(array, new Node(0), "Test Door");
        // Arrange
         double [] expected = new double[2];
        expected[0] = 5;
        expected[1] = 5;
        // Act
         double[] actual = d.getCenter();

        // Assert
        int a = (int) (actual[0] + actual[1]);
        int e = (int)(expected[0] + expected[1]);
        Assert.assertEquals(a, e);
        */
    }

    @Test
    public void testTwo() {
        /*
        double [][] array = new double[3][2];
        for(int i = 0; i < 3; i++)
        {
            array[i][0] = 0;
            array[i][1] = 12;
        }
        Door d = new Door(array, new Node(0), "Test Door");
        // Arrange
         double [] expected = new double[2];
        expected[0] = 6;
        expected[1] = 6;
        // Act
         double[] actual = d.getCenter();

        // Assert
        int a = (int) (actual[0] + actual[1]);
        int e = (int)(expected[0] + expected[1]);
        Assert.assertEquals(a, e);
        */
    }
}
